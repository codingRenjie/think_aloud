# Lessons Learned：部署后，终端用户是如何使用网页应用的？

本文整理自多轮对话中的认知纠偏与科普，主题包括：**（一）** 前端 / 后端在「开发完成并部署上线」之后，分别扮演什么角色，以及用户浏览器在整个链路中的位置；**（二）** 从本机开发到云端服务器、Git/GitHub、忽略规则、以及 PM2 / Nginx 等部署实践要点。

---

## 1. 先澄清两个容易混在一起的词

| 说法 | 含义 |
|------|------|
| **前端（开发视角）** | 你写的页面与逻辑：HTML/CSS/JavaScript（例如 React 应用）。 |
| **浏览器** | 下载并**运行**这些代码、渲染界面、发起网络请求的**客户端环境**。 |

浏览器不会自带你的 Think_Aloud 安装包；用户必须先**从某个网址下载**到你的前端构建结果（`index.html`、打包后的 `.js`、`.css` 等），页面才能出现。

---

## 2. 「只有后端要部署」吗？

**不完整。** 更准确的说法是：

- **后端**：部署在云端（或某台长期在线的机器）上，跑 API、访问密钥、调大模型等，**执行业务逻辑**。
- **前端构建产物**：同样是文件，必须放在**能被公网用 HTTP 访问**的地方（**前端托管**），用户打开你的域名时，浏览器才能把这些文件下载下来。

常见做法包括：与 API **同一台机**上用 Nginx/Express 顺带提供静态目录；或**分开**用静态托管（CDN、对象存储、Vercel/Netlify 等）专门发页面。无论哪种，**「让用户拿到前端文件」**都需要一处托管，而不是「只有浏览器、没有来源」。

**一句话**：浏览器是前端的**运行环境**；云端还要有一处**托管前端构建产物**，用户才能通过固定网址打开你的应用。

---

## 3. 用户从打开网址到能「用起来」——整条脉络

下面按时间顺序描述**典型单页应用（SPA）+ 独立 API**（与 Think_Aloud 类似）的情形。

### 第一步：拿到「壳」和前端程序

1. 用户在浏览器里输入你的网址（或点击链接），例如 `https://你的域名/`。
2. 浏览器向**提供静态资源的那一侧**（前端托管或同源下的静态目录）请求页面。
3. 服务器返回 `index.html` 以及引用的 JS/CSS；浏览器下载并执行 JS，界面初渲染完成。

到这一步为止，**前端托管**的主要任务已经体现：**把打包好的静态资源交给浏览器**。

### 第二步：业务交互（例如对话）

1. 页面里的 JavaScript 向**后端 API** 发起请求（`fetch`、SSE 流式接口等）。  
2. 后端执行业务（例如调用大模型 API），把结果**经 HTTP/SSE（或 WebSocket）写回**。  
3. 浏览器里的 JS 收到数据后更新 DOM，用户看到回复。

**要点**：对话、大模型返回等内容，走的是 **浏览器 ↔ 后端** 这条链路。**不是**「后端 → 前端托管 → 再由前端托管推给浏览器」这种「业务数据必须经过静态站」的模式。

**前端托管**通常**不参与**每一次对话数据的转发；它负责的是**更早阶段**：让用户加载并运行你的前端应用。之后是浏览器里的代码直接与后端通信。

### 第三步（可选）：同一域名下的「看起来只有一个站」

线上常用**同一域名**：前面用网关或反向代理，把 `/` 指向前端静态资源，把 `/api` 指到 Node 进程。对用户仍是一个网址；技术上仍是「静态由一处提供、API 由另一进程处理」，数据从后端经代理**回到浏览器**，而不是先到「静态文件服务器」再当业务中转站。

---

## 4. 用一张心智图收束

```
用户输入网址
    → 浏览器请求「页面与静态资源」
    → 【前端托管 / 静态资源服务】返回 HTML、JS、CSS
    → 浏览器执行 JS，界面可用

用户点击「发送」等操作
    → 浏览器里的 JS 请求【后端 API】
    → 后端执行业务（含调用大模型等）
    → 响应直接回到【浏览器】，JS 更新界面
```

---

## 5. 和本轮对话中曾出现的误解对照

| 误解 | 更正 |
|------|------|
| 前端就是用户的浏览器，不需要部署到云端 | 浏览器是**运行**环境；**前端构建产物**必须从某台对外服务上下载，因此要「托管」。 |
| 大模型结果要先到前端托管，再到浏览器 | 业务响应一般是 **后端 → 浏览器**；前端托管不负责逐次转发对话内容。 |

---

## 6. 与「历史记录存在哪」的补充（同轮相关话题，简记）

历史对话若存在 **`localStorage`**，则数据在**当前这一浏览器实例**的本地存储里；Chrome、Safari、IDE 内置浏览器等**彼此隔离**，同一网址也会是不同存储分区，因此列表可能不一致。这与「部署后静态/API 如何分工」是不同维度的问题，但都与「用户侧实际发生什么」有关。

---

## 7. 从本机开发到云端持续服务（部署视角总览）

以下是在探索 Think_Aloud 部署过程中的学习要点，经补充后与上文衔接。

### 7.1 本机能跑 ≠ 公网能访问

在 Mac 上「开发环境能跑」通常指：**前端开发服务器（如 Vite）+ 本机 Node 后端**同时开着，浏览器访问 `localhost` 或本机端口，页面与 API 都在**同一台个人电脑**上闭环。

这与「对外提供服务」的差别在于：

- **公网用户**无法直接访问你家里的电脑（运营商、防火墙、无固定公网 IP 等限制）。
- **域名**解析到的是**云端服务器的公网 IP**；用户浏览器连的是**机房里的机器**，不是你的 Mac。

因此「下一步」的典型路径是：把**已通过测试的代码**放到 **7×24 在线的云服务器**上，在那里安装依赖、构建前端、配置进程与网关，使任何人通过 **HTTP(S) 与域名** 访问到同一套应用。

**Think_Aloud 的补充**：本仓库在线上可由 **同一 Node 进程** 既提供 `/api/*`，又托管 `client/dist` 静态文件（见 `server/index.js`）。因此「部署到云端」在这套架构里常体现为 **一台机、一个监听端口（如 8787）+ 前端的 Nginx 反代**；并不必然等于「前端、后端各部署两套完全不同的运行时」，但**从用户视角**仍是：先拿到页面，再调 API。

### 7.2 Git 与 GitHub：本机快照与远程备份

**Git**（仓库里的隐藏目录 `.git`）记录的是**已提交（commit）**的快照链，不是「自动实时备份整个文件夹」。

- **`git add`**：把指定改动放入**暂存区**，用于挑选「下一次提交要包含哪些内容」。
- **`git commit`**：把暂存区内容写成一次**不可变（可哈希指向）的快照**，并附说明。

因此「备份」在严格意义上指：**进入过 commit 的历史**；被 `.gitignore` 忽略的文件、以及从未 add 的本地改动，不会随 push 自动进远程。

**GitHub**（或其他 Git 托管商）提供的是**远程裸仓库 + Web 协作**：`git push` 把本机已有 **commit** 同步上去，便于异地拉取、协作与灾难恢复。常见工作流是：**本机 add/commit → push 到 GitHub**；服务器上既可用 `git clone/pull` 更新，也可用 zip 上传，但密钥（如 `server/.env`）仍应在服务器上**单独配置**，不进入 Git。

### 7.3 `.gitignore`：通常不备份哪几类文件

除你概括的两类外，可稍细分为：

| 类型 | 例子 | 原因 |
|------|------|------|
| **密钥与本地环境** | `.env`、含密钥的配置 | 防泄露；且每人、每机环境不同。 |
| **可再生的依赖目录** | `node_modules` | 体积大，可由 `package-lock.json` + `npm ci` 重现。 |
| **构建产物** | `client/dist`、`dist` | 由源码与构建命令再生；进仓库会放大 diff、易冲突。 |
| **日志与系统杂项** | `*.log`、`.DS_Store` | 与功能无关、因人而异。 |

**注意**：仅凭 `.git` 可还原的是**已跟踪且已提交**的文件；忽略项与未提交改动不在「Git 备份」范围内。

### 7.4 「部署到云端」在整体上是什么

通俗表述：**租用（或购买）一台长期联网的服务器**，在其上运行与你在本机验证过的同一套（或同源）代码，使**公网**可通过 **IP 或域名** 访问。

还需同时理解的几层（与本项目实践相关）：

- **DNS**：把域名解析到服务器 **公网 IP**；若出现 **WHOIS `clientHold`** 等状态，解析可能被注册局侧暂停，需先在域名注册商完成实名等流程。
- **云防火墙 / 安全组**：放行 **80/443**（网站入口）；**不必**对公网开放 Node 监听端口（如 8787），由 Nginx 在本机转发即可。
- **宝塔 / Nginx**：作为 **443/80 上的入口**，把请求 **反向代理** 到本机 **`127.0.0.1:8787`**。这里的「本机」指**云服务器自己**，不是你的 Mac。

### 7.5 进程守护与反向代理：PM2 与 Nginx 各自做什么

- **PM2**：在服务器上保持 **Node 进程**常驻；崩溃可自动重启；配合 **`pm2 startup`（systemd）+ `pm2 save`**，可在**整机重启**后按保存的列表自动拉起应用。日常发布代码后，需在服务器上 **重新构建**（若前端有变更）并 **`pm2 reload` 或重启进程**。
- **Nginx（反向代理）**：对外监听 **80/443**，再把请求转到 **同一台机器内** 的 Node（如 `http://127.0.0.1:8787`）。浏览器只与域名/443 交互；**TLS 证书**也通常配在 Nginx 这一层。流式接口（SSE）场景下，宜在反代配置中关闭 **`proxy_buffering`** 等，避免响应被缓冲导致聊天卡顿（参见仓库 `deploy/nginx-reverse-proxy.example.conf`）。

**易混点**：「本机」在文档里若写作 `127.0.0.1`，在部署语境下几乎总是 **云服务器回环地址**，表示 **Nginx 与 Node 同机通信**，与开发用的 Mac 无关。

---

*文件用途：个人学习笔记，便于日后回顾「部署后的用户侧链路」以及「从本机到云端的部署要点」。*
