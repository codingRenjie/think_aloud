# Think_Aloud：国内轻量云 + 宝塔 部署说明

面向「访问量很小、需要偶尔改代码再发布」的场景。与海外 PaaS 相比：数据与进程在你自己的 VPS 上，大陆访问延迟通常更低；需要多几步面板/SSH 操作。

## 1. 你需要准备什么

- **云服务器**：阿里云轻量应用服务器、腾讯云轻量等，**最低配**即可（1 核 1G 也能跑 Node）。
- **系统**：优先选 **Ubuntu 22.04 LTS**（教程多、宝塔支持好）。
- **域名（强烈建议）**：用于 HTTPS。语音相关能力在浏览器里通常要求 **https**（本地 `localhost` 除外）。
- **备案（仅当你用「中国大陆机房 + 域名对外提供网站服务」时）**：按云厂商流程办理；若暂时不想备案，可选用 **香港** 等节点的轻量（延迟与合规请自行权衡）。

## 2. 安全组与防火墙

在云厂商控制台为实例放行：

- `22`（SSH，仅你自己维护时用）
- `80`、`443`（网站访问；若只用宝塔默认端口，另按宝塔文档放行 `8888` 等面板端口，**勿对公网长期大开面板端口**，可用「仅本机 SSH 隧道访问面板」等方式收紧）

## 3. 安装宝塔面板

1. 打开 [宝塔面板官网](https://www.bt.cn/)，按当前系统复制 **一键安装命令**，在 SSH 里执行。
2. 安装结束后记下面板地址、用户名、密码，浏览器登录。
3. 按提示安装 **Nginx**（用于反向代理与 SSL）。

> 安全提醒：尽快修改默认面板密码；面板端口勿对 `0.0.0.0/0` 长期裸奔。

## 4. 安装 Node.js 20+

任选其一：

- **宝塔「软件商店」**：搜索 **Node 版本管理器** 或官方提供的 Node 环境，安装 **20.x LTS**。
- **SSH 自行安装**：使用 [NodeSource](https://github.com/nodesource/distributions) 或 nvm，保证 `node -v` ≥ 20。

再安装 PM2（进程守护，崩溃自动拉起）：

```bash
npm install -g pm2
```

## 5. 把代码放到服务器

推荐目录：`/www/wwwroot/think-aloud`（与宝塔网站目录习惯一致，可自定）。

```bash
cd /www/wwwroot
git clone <你的仓库地址> think-aloud
cd think-aloud
```

若无 Git，可用宝塔「文件」上传压缩包后解压到同路径。

## 6. 配置环境变量（API 密钥）

**不要**把含密钥的 `.env` 提交到 Git。在服务器上新建或编辑：

`think-aloud/server/.env`

内容与本地开发一致，至少包含其一：

- `OPENAI_API_KEY` 或 `MOONSHOT_API_KEY`（及你实际使用的 `OPENAI_BASE_URL`、`OPENAI_MODEL` 等，见仓库内 `server/index.js` 顶部注释逻辑）。
- `OPENAI_MODEL` 请用当前可用模型，例如 `kimi-k2.6`。`moonshot-v1-32k`、`kimi-k2.5` 已于 2026-08-31 下线，继续用会返回 404。改完 `.env` 后执行 `pm2 restart think-aloud`。

## 7. 首次构建与启动

在 **`think-aloud` 项目根目录**执行：

```bash
npm ci
npm run build
pm2 start ecosystem.config.cjs
pm2 save
```

说明：

- `npm run build` 会构建前端并安装服务端依赖（与仓库根目录 `package.json` 中脚本一致）。
- `ecosystem.config.cjs` 让进程在 `server/` 目录下跑 `node index.js`，默认监听 **8787** 端口（与文件内 `PORT` 一致）。若你改了端口，宝塔反代目标要一起改。

验证本机进程：

```bash
curl -sS http://127.0.0.1:8787/api/health
```

应返回 JSON，且 `hasKey` 为 `true` 表示密钥已读到。

## 8. 宝塔里配置网站与 HTTPS

1. **网站** → **添加站点**，域名填你的域名（可先填假域名仅做反代测试，再改）。
2. 进入该站点 → **设置** → **反向代理** → **添加反向代理**：
   - 目标 URL：`http://127.0.0.1:8787`
   - 发送域名：`$host`
3. 聊天 **SSE 流式**需要关闭代理缓冲（若宝塔高级配置里可改 Nginx，可参考仓库内 [`deploy/nginx-reverse-proxy.example.conf`](deploy/nginx-reverse-proxy.example.conf) 中的 `proxy_buffering off` 等片段，合并进站点配置）。
4. **SSL**：同一站点设置里申请 **Let’s Encrypt**（需域名已解析到本机公网 IP）。

完成后用浏览器访问 `https://你的域名/`，应能打开 Think_Aloud 页面。

## 9. 以后如何更新版本

在服务器项目根目录：

```bash
./scripts/deploy_update.sh
```

或手动：

```bash
git pull
npm ci
npm run build
pm2 reload ecosystem.config.cjs --update-env
```

## 10. 常见问题

| 现象 | 排查 |
|------|------|
| 502 Bad Gateway | PM2 是否在跑、`curl 127.0.0.1:8787` 是否通、宝塔反代端口是否一致 |
| 页面空白 / 404 | 是否执行过 `npm run build`（必须有 `client/dist`） |
| 聊天报错 | `server/.env` 密钥与模型配置是否正确；看 `pm2 logs think-aloud` |
| iOS 语音不工作 | Web Speech API 在 Safari 上能力有限，属浏览器限制，与服务器无关 |

---

若你希望后续再支持「无宝塔、纯 systemd + nginx」的最小安装，可在同目录另加脚本，流程与上文类似，仅去掉面板步骤。
